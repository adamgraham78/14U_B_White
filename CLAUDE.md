# Working with Adam on this project

## Response style
- **Keep responses short.** Lead with the answer, cut the preamble.
- **One thing at a time.** Adam works sequentially — present one decision or one
  chunk of work per response, not a menu. Don't stack multiple open questions.
- Don't re-explain what's already in the week map.

## Project
14U hockey curriculum. See `14u-week-map.md` for the schedule and
`scripts/build_site.py` for the plan data.

**No roster, player, or team-specific information in this repo.** Skills and
drills only. Planning notes with team detail stay local and gitignored.

## Where things stand

- `docs/` is generated. Edit the data structures at the top of
  `scripts/build_site.py` (`WEEKS`, `THEMES`, `PRACTICE`, `AREAS`), then run
  `python3 scripts/build_site.py`. Never hand-edit `docs/` - with one
  exception the build does not touch: `docs/style.css`.
- **Three layers, don't mix them.** The area page is the *vision* - principles,
  focus areas, cues, errors. The Season Plan is the *plan* derived from it. A
  logged practice is the *record*. The area page must not restate weekly detail.
- The per-segment strings in `WEEKS` are unpublished planning data used to build
  practices in IHS; only the area tag reaches the site, as a badge. Real drill
  names appear on the Season Plan once a practice is logged.
- `data/drills.csv` is generated by `normalize_drills.py` from the untouched IHS
  export. Non-IHS drills go in its `EXTRA_DRILLS` list, never into either CSV.
- Use **USA Hockey's own terminology** for checking (contact confidence, angling,
  stick checks, body blocks, body checks). Source manual is in Adam's Drive.
- Only the **Contact & Checking** area page is published. The other four are
  written but held back for review - flip `"published"` in `AREAS` to release one.
  Their cards still show on the published Overview, so card text is public.
- Practices are logged from pasted IHS text, not scraped. See README.
- Push with the token in `.env` (gitignored):
  `AUTH=$(printf 'x-access-token:%s' "$GITHUB_TOKEN" | base64)`
  `git -c http.extraheader="AUTHORIZATION: Basic $AUTH" push origin main`

## Next up

Contact & Checking is done and pushed. Open items, in rough priority:

1. **Twigs Body Contact** and **Bump Back** are in the drill library but have no
   link or description - they came from the USA Hockey manual, not IHS. They are
   the only reps at *taking* a check, so a coach needs the how-to before
   Practice 1. Needs a notes field for linkless drills.
2. Forecheck approach is **1-2-2 or 2-1-2, TBD** - and that TBD is visible on the
   public Overview card.
3. Build Practice 2 (Week 2: contact on the wall), then log it.
